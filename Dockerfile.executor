FROM kgyrtkirk/hive-dev-box:latest

USER root

COPY etc/* /etc/
COPY bin/* /bin/
RUN userdel dev
RUN groupadd -g 1000 jenkins
RUN useradd -m -o -u 1000 -g 1000 -d /home/jenkins -s /bin/bash jenkins

RUN tools/install_executor.bash

WORKDIR /home/jenkins
USER jenkins
ENTRYPOINT []
CMD ["bash"]
